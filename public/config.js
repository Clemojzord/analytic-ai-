/**
 * Analytic AI — API Configuration
 *
 * HOW TO UPDATE AFTER DEPLOYING TO RENDER:
 * 1. Copy your Render URL (e.g. https://analytic-ai-backend.onrender.com)
 * 2. Replace the value of API_URL below
 * 3. Run: firebase deploy
 */
const CONFIG = {
    // ▼ Replace with your Render URL after deploying the backend ▼
    API_URL: 'https://analytic-ai-backend.onrender.com'
};
